#include <stdio.h>
#include <stdlib.h>
#define M 256

int main(int argc, char* argv[])
{
	int a, b;
	char buffer[M];

	while (1)
	{
		fgets(buffer, M, stdin);
		sscanf(buffer, "%d %d", &a, &b);
		printf("Sum is %d\n", a + b);	
	}
}
