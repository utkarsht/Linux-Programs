#include <bits/stdc++.h>
using namespace std;

struct edge
{
	int a, b;
	int part;
	double mul;
};

struct state
{	
	int id;
	int size;
	vector<edge> e;
	state(int id, int size) : id(id), size(size)
	{}
};

int main()
{
	
	freopen("E://")
}
